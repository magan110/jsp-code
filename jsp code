✅ Quick Fix 1 — Force HF to avoid Xet bridge

This keeps downloads on the regular Hugging Face CDN (which usually validates fine).

Add these lines at the very top of your script (before any HF/transformers import):
